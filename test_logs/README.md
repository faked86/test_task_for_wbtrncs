# Tests for logs() function

## Requrements

Installed and running Docker

- Build test image: `docker build -t hello .`
- Run test container: `docker run --name hello hello`

Python libraries:
- `pip install pytest-asyncio`
- `pip install aiohttp`

## Usage
Simply run `pytest`
